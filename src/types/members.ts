export interface TokenInfo {
  accessToken: string;
  refreshToken: string;
}

export interface UserInfo {
  name: string;
  studentNumber: string;
  dormType: string;
  college: string;
  penalty: number;
}
